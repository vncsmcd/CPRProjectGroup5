// FUNDAMENTALS MODULE HEADER

#ifndef FUNDAMENTALS_H_ // Checks if FUNDAMENTALS_H_ is not defined
#define FUNDAMENTALS_H_ // If not defined, defines FUNDAMENTALS_H_

#include <stdio.h> // Includes the standard input/output library
#include <stdlib.h> // Includes the standard library for functions like memory allocation (e.g., malloc)
#include <string.h> // Includes the string manipulation library

void fundamentals(void); // Declares a function prototype for the fundamentals function that takes no arguments and returns nothing

#endif // Ends the conditional compilation directive